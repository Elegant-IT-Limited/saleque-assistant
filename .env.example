# Postgres 17 with pgvector. Tests and scripts create and drop the schema, so point this at a scratch database.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Only needed to run against live providers. The test suite never reads these.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
TYPESAFE_API_KEY=
